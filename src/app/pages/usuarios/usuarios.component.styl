@import "../../../assets/styles/vars"

.no-usuarios
  display flex
  flex-direction column
  align-items center
  width 100%
  color primaryBack

.loading-cell
  display flex
  justify-content center
  padding 1em 0

.descripcion-cell
  padding 0
  width 100%
  flex-direction: row;
  span
    display flex
    width 100%
    justify-content center
    align-items center
    align-content center
    flex-direction column
    &:first-of-type
      padding-left 24px
    &:last-of-type
      padding-right 24px
